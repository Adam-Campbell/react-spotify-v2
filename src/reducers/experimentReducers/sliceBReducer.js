
const defaultState = {
    sliceName: 'B',
    position: 'second'
};

const sliceBReducer = (state=defaultState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default sliceBReducer;