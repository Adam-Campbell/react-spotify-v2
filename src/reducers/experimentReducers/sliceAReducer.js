
const defaultState = {
    sliceName: 'A',
    position: 'first'
};

const sliceAReducer = (state=defaultState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default sliceAReducer;